# File upload
# Route not found response(DONE)
# Validation(DONE)
# Needs abstraction on: (DONE)
  1.separate modules(e.g users, posts)(DONE)
  2.break modules into struct and handler files(DONE)
  3.custom validation rules for common use(DONE)
  4.validation for query strings(DONE)
  5. abstract formatter(break into separate ones) to those folders(DONE. Decided against it due to reasons)
# Handle JSON marshalling error(DONE)
# store encrypted/hashed password instead of raw password(DONE)
# Go for a better abstraction for dealing with and logging errors globally(DONE)
# Circular dependency concern
# user registration & login route with access and refresh tokens
# Add rate limiter(DONE)
# Add cors(DONE)
# Global error handler and logger(DONE but might need a notepad version. Also, not sure if a request logger is needed)
# Add custom exception handler(DONE added custom error responses)
# Relations
# Nested Relations
# Pagination for getAll calls(DONE)
# DB Transactions when disjointing relational data
# Authorization and Access Control
# PDF/Invoice generation
# Scheduled Tasks/ CRON job
# Formatted responses
# Redis
# Queue (BullMQ + Redis)(e.g for email delivery) + Implement email service(NEEDS RIGOROUS TESTING. TS PROBLEMS.)
# Add health check endpoint(DONE)
# Add request logging middleware


Possible path:  
# Learn nginx as reverse proxy + load balancer
# Learn CI/CD (GitHub Actions)
# Learn Kubernetes basics
# Learn microservices with NestJS
# Learn CQRS pattern
    ↓
  Months
    ↓
# Learn AWS/GCP basics (EC2, RDS, ElastiCache, S3)
# Learn monitoring (Prometheus + Grafana)
# Learn distributed tracing (OpenTelemetry)